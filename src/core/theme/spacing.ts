/** Dense 8px grid. */
export const spacing = (multiplier: number) => multiplier * 8;

/** Max corner radius allowed anywhere in the UI — no pill buttons, no rounded cards. */
export const radius = 2;
